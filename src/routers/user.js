const express = require('express')
const router = express.Router()
const userController = require('../controllers/userController')
const userMiddleware = require('../helpers/users')
const { sendResponse } = require('../helpers/response')
const { uploadMidleware } = require('../helpers/uploadMidleware')

router.route('/').get(userMiddleware.isLoggedIn, userController.getUserById, sendResponse)
router.route('/edit-profile').patch(userMiddleware.isLoggedIn, userController.getUserById, uploadMidleware, userController.editProfile, sendResponse)
router.route('/update-password').post(userMiddleware.isLoggedIn, userController.updatePassword)
router.route('/forgot-password').post(userController.forgetPassword)
router.route('/login').post(userMiddleware.validateLogin, userController.login)
router.route('/logout').get(userMiddleware.isLoggedIn, userController.logout)
router.route('/sign-up').post(userMiddleware.validateSignupSeller, userController.signUp)
router.route('/verifikasi-email/:token').get(userController.verifikasiEmail)
router.route('/customer-address')
  .post(userMiddleware.isLoggedIn, userController.insertAddress, sendResponse)
  .get(userMiddleware.isLoggedIn, userController.getAllCustomerAddress, sendResponse)
//   .delete()
module.exports = router
